# v0.1.0 verification

## Current multipart-copy and Git changes — 2026-09-18

Final main-run results, reported by the maintainer for this documentation update (not a new execution of the suites by this docs-only update):

- **Unit:** `pnpm test`: 112 passed, 0 skipped.
- **Type checks:** `pnpm check:types` and `pnpm check:examples` passed.
- **Integration:** `VOLUMES_TEST_BOOTSTRAP=1 pnpm test:integration`: 26 passed, 0 failed, 0 skipped, including a small multipart fixture, real-FUSE Git, minimum/pinned rclone 1.68.0/1.75.1 coverage and bare-Ubuntu bootstrap.
- **Multipart scope:** a small real multipart fixture plus mocked large-size boundaries; **no actual 5 TiB object copy was tested**. These results are not a production-scale throughput or all-provider compatibility claim.
- **Git scope:** unit tests, local smart HTTP and real-FUSE Git. **No live authenticated GitHub tests** are claimed; this does not establish blanket Git repository compatibility.
- **Freestyle live:** `pnpm test:freestyle`: 1 skipped because `FREESTYLE_API_KEY`, `VOLUMES_S3_BUCKET`, `VOLUMES_S3_ACCESS_KEY_ID` and `VOLUMES_S3_SECRET_ACCESS_KEY` were missing. No live Freestyle round trip or pause/resume validation.

The earlier 85/17 counts below remain historical. The current-code [local MinIO rerun](performance-local.md#current-code-rerun-mandatory-source-head-safety-checks) on **2026-09-18** observed **2.86x metadata-list** and **2.97x clone** ratios of elapsed medians at concurrency 1 versus 8. It includes mandatory source HEAD checks but uses only small single-copy objects: these are local host API measurements, not multipart throughput, Git, FUSE or Freestyle performance, or general speedup guarantees. Exact medians, source hashes and the older historical run are retained in the benchmark evidence. See [multipart settings/permissions](../performance.md#single-and-multipart-copy), [failure reconciliation](../semantics.md#clone-publication-and-reconciliation) and [Git API/trust boundary](../git.md). Type checks do not type-check Markdown snippets. No lint command is configured.

## Historical clone and performance changes — 2026-09-18

Pre-multipart/Git command results dated **2026-09-18**, not certification of the current expanded suite:

- **Unit:** `pnpm test`: 85 passed.
- **Type checks:** `pnpm check:types` and `pnpm check:examples` passed.
- **Integration:** `VOLUMES_TEST_BOOTSTRAP=1 pnpm test:integration`: 17 passed, 0 failed, 0 skipped, including the nested rclone 1.68.0/1.75.1 tuning tests and bare-Ubuntu bootstrap. This supersedes the prior safety-only result below.
- **Freestyle live:** `pnpm test:freestyle`: 1 skipped because `FREESTYLE_API_KEY`, `VOLUMES_S3_BUCKET`, `VOLUMES_S3_ACCESS_KEY_ID` and `VOLUMES_S3_SECRET_ACCESS_KEY` were missing. No Freestyle round trip, pause/resume or performance claim is established.
- **Historical local benchmark:** the earlier Docker-backed MinIO run, raw samples, source hashes, timing boundaries, integrity checks and cleanup evidence are retained in the [historical archive](performance-local.md#historical-evidence-archive-1951-run-not-current-code-results). That run yielded 2.94x metadata-list and 2.46x clone ratios of elapsed medians at concurrency 1 versus 8; these are not the current-code rerun's results or Freestyle/FUSE throughput.

That historical run's unit coverage added clone publication visibility, same-name races, ambiguous publication retention, late remote-copy completion after failed/empty cleanup, source ETag checks, manifest count/byte budgets, v1/v2 validation, generation-specific cache identity, bounded list concurrency and mount-option validation/wiring. See [`clone.test.mjs`](../../test/unit/clone.test.mjs), [`registry.test.mjs`](../../test/unit/registry.test.mjs), [`volumes.test.mjs`](../../test/unit/volumes.test.mjs) and [`validate.test.mjs`](../../test/unit/validate.test.mjs). Real-rclone tuning tests passed on 1.68.0 and 1.75.1 in the complete integration run; they check option acceptance/readback and integrity, not speed.

These changes retain the unmount-first, retained-VFS drain safety protocol. See [performance/API details](../performance.md) and [source-linked research rationale](../freestyle-research.md). `git diff --check` passed; no lint command is configured.

## Prior safety-only verification — 2026-09-18

Historical results before the later clone/performance changes, superseded by the complete run above:

- **Unit:** `pnpm test`: 58 passed, zero skipped, up from the 33-test baseline below.
- **Type checks:** `pnpm check:types` and `pnpm check:examples` passed.
- **Integration:** `VOLUMES_TEST_BOOTSTRAP=1 pnpm test:integration`: 13 passed, zero failed, zero skipped, including forced-detach recovery after rejected reattach in `test/integration/rclone-guest.test.mjs`. This final run supersedes the earlier 12-pass safety-change run.
- **Freestyle live:** one test skipped because `FREESTYLE_API_KEY`, `VOLUMES_S3_BUCKET`, `VOLUMES_S3_ACCESS_KEY_ID` and `VOLUMES_S3_SECRET_ACCESS_KEY` were missing. No Freestyle round trip or pause/resume behavior was validated.

That safety-only run's passing regression coverage addresses external unmount before drain, waiting for FUSE serving to stop, retained VFS upload completion, late writes, lifecycle locking, process identity guards, strict counter parsing, storage-aware cache identity, registry validation and atomic volume creation. The forced-detach test verifies that a rejected reattach preserves stopped-process evidence and pending cache, and that a later same-identity reattach can drain it.

`FLUSH_FAILED` can now leave an unmounted but live uploader with retained cache/state for retry. Forced uncertain detach retains recovery state in place and keeps the advisory attachment record. The historical results below describe the earlier implementation, not these changed semantics. No secrets or private logs are included in this record.

## Historical baseline — 2026-09-17

The results below were run on a macOS host with Docker Desktop 27.3.1 (Linux 6.8.0-47-generic aarch64 VM), or in the linked CI jobs. They are historical evidence only and do **not** certify current uncommitted code. Nothing below was run on Freestyle.

### Unit tests (mocked sandbox, in-memory object store)

`pnpm test`: 33 tests, 33 pass. Covers input validation (names, mount paths, subpaths, prefixes, buckets, shell quoting, protected roots), storage config resolution and rclone environment generation, storage error classification, the registry (create/get/list/delete, namespaces, attachment gating, corrupt records), generated guest scripts (parsed with `sh -n`, no credential strings, pinned rclone version and checksums), guest output parsing, and error mapping for every bootstrap, mount and detach failure code. `pnpm check:types` compiles the real `freestyle@0.2.13` client and `Vm` against the adapter's structural types. `pnpm check:examples` type-checks both examples.

### Linux integration tests (Docker + MinIO, real rclone FUSE mounts; historical)

`pnpm test:integration`: 10 tests, 10 pass, 26 s. Sandboxes are `rclone/rclone:latest` (Alpine, rclone 1.75.1) containers with `--device /dev/fuse --cap-add SYS_ADMIN`; storage is `quay.io/minio/minio:latest`. The library's `dockerSandboxes()` adapter runs the same guest scripts the Freestyle adapter would.

| Test | Result |
| :--- | :--- |
| create, attach, write, flush, detach, reattach in a fresh container, replacement seen by the first container, idempotent attach/detach | pass; objects verified in the bucket (`hello.txt`, `dir/nested.txt`, `emptydir/` marker); credentials absent from every `/proc/*/cmdline` |
| read-only attach | pass; write fails with "Read-only file system", mismatched read-only re-attach at the same path refused |
| namespace and subpath isolation | pass; two prefixes with same-named volumes stay separate; a `users/alice` subpath mount cannot reach `users/bob`; deleting one tenant's volume leaves the other's data |
| invalid credentials and unavailable storage | pass; `STORAGE_AUTH` and `STORAGE_UNREACHABLE` raised on the host before any sandbox exec, no mount state created |
| mount failure and timeout inside the sandbox | pass; unresolvable endpoint → `MOUNT_FAILED` with log tail; blackhole endpoint → `MOUNT_TIMEOUT` within the bound, process killed, mount point cleaned, inspect reports `absent` |
| sandbox without /dev/fuse | pass; `FUSE_UNAVAILABLE` before any install |
| interrupted writes | pass; write buffered (`uploads.queued = 1`), rclone killed with SIGKILL, inspect → `stale`, object absent from the bucket, plain detach refused with `MOUNT_STALE`, re-attach resumes the cached upload, detach → `flushed: true`, object content verified |
| busy mount | pass; open file handle → `MOUNT_BUSY`, mount stays; `force` detaches with `flushed: false` |
| flush failure | pass; MinIO paused → `FLUSH_FAILED`, mount still attached; MinIO resumed → detach flushes, object present |
| delete | pass; refused with `VOLUME_IN_USE` while attached, then removes only that volume's prefix |

`VOLUMES_TEST_BOOTSTRAP=1`: the bare `ubuntu:24.04` bootstrap test passes in 17 s: apt installs `fuse3` and `curl`, rclone 1.75.1 is downloaded from downloads.rclone.org and its SHA-256 verified, the mount comes up, a write is flushed to the bucket on detach.

### Manual probes that shaped the original design (historical, not part of the suite)

Before writing the library, four shell probes ran the raw mechanics in the same Docker setup and established: rclone's `--daemon` mode cannot host the rc socket (bind conflict), so rclone is backgrounded with `setsid` and a pid file; a killed rclone leaves a stale FUSE entry that fools `/proc/mounts`-only readiness checks and a stale socket file that blocks restarts; `vfs/queue-set-expiry` with `expiry=0` uploads immediately; a clean `fusermount3 -u` makes rclone exit and remove its socket; `--allow-other` lets non-root users write, `--uid/--gid` change the reported owner; bad credentials and unreachable endpoints fail at rclone startup; on restart with the same cache directory rclone re-queues dirty files.

### Historical CI (GitHub Actions, `ubuntu-latest`, x86_64)

The first pull request ran both workflow jobs twice (push and pull_request events), all green for that earlier revision. These runs do not certify the current safety changes:

- `verify` (unit tests, SDK type check, examples, `pnpm pack`): [run 35272270095](https://github.com/reachjalil/freestyle-volumes/actions/runs/35272270095/job/105374279085), [run 35272274879](https://github.com/reachjalil/freestyle-volumes/actions/runs/35272274879/job/105374292922)
- `integration` (the 10 Docker + MinIO tests plus the bare-Ubuntu bootstrap test with `VOLUMES_TEST_BOOTSTRAP=1`): [run 35272270095](https://github.com/reachjalil/freestyle-volumes/actions/runs/35272270095/job/105374279295), [run 35272274879](https://github.com/reachjalil/freestyle-volumes/actions/runs/35272274879/job/105374293283)

The x86_64 job exercises the amd64 rclone download and checksum; the local run above exercised arm64.

## Not verified on Freestyle

- **Freestyle itself, including pause/resume.** The live tier was skipped for the missing variables listed above. SDK type checks and Docker results do not establish Freestyle runtime behavior. Assumptions are listed in `docs/freestyle.md`.
